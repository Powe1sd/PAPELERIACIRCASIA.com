// Obtén el elemento de la sección que deseas ocultar por su id
const bisuteriaSection = document.getElementById("bisuteriaProducts");

// Verifica si el elemento existe antes de intentar ocultarlo
if (bisuteriaSection) {
  bisuteriaSection.style.display = "none";
}
